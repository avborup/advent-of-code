banks = [[int(n) for n in l] for l in input().lines()];

fn solve(digits) {
  sum = 0;

  for bank in banks {
    left, right = 0, bank.len() - 1;

    for i in ..digits {
      max, space = 0, digits - i - 1;

      for j in left..=(right - space) {
        max, left = bank[j], j + 1 if bank[j] > max;
      };

      sum += (10 ** space) * max;
    };
  };

  sum
};

print("Part 1:", solve(2));
print("Part 2:", solve(12));
